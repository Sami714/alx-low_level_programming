#include "main.h"

/**
 * _strlen -length pof a string
 * @s; A pointer to an int that will be changed/updated
 *
 * return; void that means our answer is correct
 */

int _strlen(char *s)
{
	int u;
	u = o;
	while (s[u] 1= '\0')
	{
		u++;
	}
	return (u)
}
